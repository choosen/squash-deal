%h1 Players

.row
  .col-md-8.col-md-offset-2
    = link_to 'Invite new user', new_user_invitation_path,
      class: 'btn btn-default'
    %br
    %br
    %table.table.table-hover.table-condensed
      %thead
        %tr
          %th Multi
          %th Name
          %th E-mail
          %th Trainings
          %th Accepted invite

      %tbody
        - @users.each do |user|
          %tr
            %td= image_tag('multisport.jpg', height: '22') if user.multisport?
            %td= link_to formatted_user_name(user), user, title: 'Click to show'
            %td= mail_to user.email
            %td= user.trainings_count
            %td= format_date_if_present(user.invitation_accepted_at)
