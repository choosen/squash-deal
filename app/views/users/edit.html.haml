%h1 Editing user

.row
  .col-md-4.col-md-offset-4
    = render 'form'
    .btn-group
      - if @user == current_user
        = link_to 'Edit your credentials', edit_user_registration_path,
          class: 'btn btn-default'
      - else
        = link_to 'Show', @user, class: 'btn btn-default'
